# ExtractAll
Extracts various file types with one command

## Supported file formats
 * zip
 * tar.gz
 * gz

## Requirements
 * unzip
 * tar
 * gunzip

## usage
 1. install
```
sudo bash extractall.sh install
```
 2. use
 ```
 extractall test.zip
 ```
